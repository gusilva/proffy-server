version: "3.3"
services:
  swagger-editor:
    image: swaggerapi/swagger-editor
    container_name: "swagger-editor"
    environment:
      SWAGGER_FILE: /tmp/swagger.yaml
    volumes:
    - ./src/swagger:/tmp
    ports:
      - "8081:8080"
